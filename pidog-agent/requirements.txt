# LiveKit + Gemini Agent (runs on Mac/Cloud, not Pi)
livekit-agents[google,images]>=1.2.0
python-dotenv>=1.0.0

# HTTP client for communicating with Pi hardware server
requests>=2.31.0

# Image processing (for handling camera frames from Pi)
opencv-python>=4.8.0
numpy>=1.24.0
